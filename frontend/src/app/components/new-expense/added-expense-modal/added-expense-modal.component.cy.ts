import { AddedExpenseModalComponent } from './added-expense-modal.component'

describe('AddedExpenseModalComponent', () => {
  it('should mount', () => {
    cy.mount(AddedExpenseModalComponent)
  })
})